services:
  - type: web
    name: dynamic-delivery-slot-scheduling-system
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.2
      - key: FLASK_SECRET_KEY
        generateValue: true
